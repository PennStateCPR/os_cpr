This folder contains documentation about how to build, run, and install
the Penn State Central Person Registry (CPR).

To test the CPR by using the QuickStart, refer to the QuickStart_Instructions
for information on how to retrieve the quickstart and set it up.